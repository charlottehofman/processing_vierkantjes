void setup()
{
  size(900, 900);
  background(255);
  smooth();
  fill(000);
  noStroke();
  rect(450, 300, 40, 40);
  
  pushMatrix();
  // move the origin to the pivot point
  translate(420, 314); 
  
  // then pivot the grid
  rotate(radians(40));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
   pushMatrix();
  // move the origin to the pivot point
  translate(425, 370); 
  
  // then pivot the grid
  rotate(radians(90));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  pushMatrix();
  // move the origin to the pivot point
  translate(450, 440);
  
  // then pivot the grid
  rotate(radians(140));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  pushMatrix();
  // move the origin to the pivot point
  translate(450, 440);
    
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  fill(000);
  noStroke();
  rect(450, 439, 40, 40);
  
   pushMatrix();
  // move the origin to the pivot point
  translate(546, 435);
  
  // then pivot the grid
  rotate(radians(130));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  fill(000);
  noStroke();
  rect(514, 369, 40, 40);
  
   pushMatrix();
  // move the origin to the pivot point
  translate(546, 344);
  
  // then pivot the grid
  rotate(radians(140));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  //BREAK
  
  
    pushMatrix();
  // move the origin to the pivot point
  translate(380, 280); 
  
  // then pivot the grid
  rotate(radians(40));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  pushMatrix();
  // move the origin to the pivot point
  translate(348, 322); 
  
  // then pivot the grid
  rotate(radians(18));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
   pushMatrix();
  // move the origin to the pivot point
  translate(374, 370); 
  
  // then pivot the grid
  rotate(radians(90));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  pushMatrix();
  // move the origin to the pivot point
  translate(335, 420); 
  
  // then pivot the grid
  rotate(radians(-20));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  pushMatrix();
  // move the origin to the pivot point
  translate(411, 473);
  
  // then pivot the grid
  rotate(radians(140));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  pushMatrix();
  // move the origin to the pivot point
  translate(450, 440);
    
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  fill(000);
  noStroke();
  rect(450, 439, 40, 40);
  
   pushMatrix();
  // move the origin to the pivot point
  translate(546, 435);
  
  // then pivot the grid
  rotate(radians(130));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  fill(000);
  noStroke();
  rect(514, 369, 40, 40);
  
   pushMatrix();
  // move the origin to the pivot point
  translate(546, 344);
  
  // then pivot the grid
  rotate(radians(140));
  
  // and draw the square at the origin
  fill(0);
  rect(0, 0, 40, 40);
  popMatrix();
  
  
  
  
  
}
